import { DiscordLinkDirective } from './discord-link.directive';

describe('DiscordLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscordLinkDirective();
    expect(directive).toBeTruthy();
  });
});
