import { CharacterAvatarSrcPipe } from './character-avatar-src.pipe';

describe('CharacterAvatarSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterAvatarSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
