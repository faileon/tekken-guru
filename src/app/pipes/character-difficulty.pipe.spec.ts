import { CharacterDifficultyPipe } from './character-difficulty.pipe';

describe('CharacterDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
