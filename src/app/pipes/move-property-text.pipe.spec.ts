import { MovePropertyTextPipe } from './move-property-text.pipe';

describe('MovePropertyTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MovePropertyTextPipe();
    expect(pipe).toBeTruthy();
  });
});
