import { ComboNotationPipe } from './combo-notation.pipe';

describe('ComboNotationPipe', () => {
  it('create an instance', () => {
    const pipe = new ComboNotationPipe();
    expect(pipe).toBeTruthy();
  });
});
