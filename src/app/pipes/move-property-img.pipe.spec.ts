import { MovePropertyImgPipe } from './move-property-img.pipe';

describe('MovePropertyImgPipe', () => {
  it('create an instance', () => {
    const pipe = new MovePropertyImgPipe();
    expect(pipe).toBeTruthy();
  });
});
