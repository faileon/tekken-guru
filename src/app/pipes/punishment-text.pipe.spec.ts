import { PunishmentTextPipe } from './punishment-text.pipe';

describe('PunishmentTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PunishmentTextPipe();
    expect(pipe).toBeTruthy();
  });
});
