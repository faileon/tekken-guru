import { InputPathPipe } from './input-path.pipe';

describe('InputPathPipe', () => {
  it('create an instance', () => {
    const pipe = new InputPathPipe();
    expect(pipe).toBeTruthy();
  });
});
