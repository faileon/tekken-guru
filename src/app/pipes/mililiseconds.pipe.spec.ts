import { MililisecondsPipe } from './mililiseconds.pipe';

describe('MililisecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new MililisecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
