import { HitDamagePipe } from './hit-damage.pipe';

describe('HitDamagePipe', () => {
  it('create an instance', () => {
    const pipe = new HitDamagePipe();
    expect(pipe).toBeTruthy();
  });
});
