import { ThumbnailPathPipe } from './thumbnail-path.pipe';

describe('ThumbnailPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailPathPipe();
    expect(pipe).toBeTruthy();
  });
});
