import { ComboNamePipe } from './combo-name.pipe';

describe('ComboNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ComboNamePipe();
    expect(pipe).toBeTruthy();
  });
});
